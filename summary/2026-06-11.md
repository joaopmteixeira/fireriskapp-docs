# Resumo do dia - 11 de junho de 2026

## O que foi feito hoje

- **Imagem Docker corrigida e verificada (INFRA-03):** a versão de Python usada na imagem foi atualizada para a 3.12 (a 3.14 não tinha imagem oficial estável), as dependências de testes foram separadas das de produção, e todos os 342 testes passaram. A imagem final tem 526 MB.
- **Histórico do git organizado:** os commits do Docker foram isolados numa branch própria (`feat/infra03-docker`) e depois fundidos corretamente na branch principal, seguindo o processo habitual do projeto.
- **Credenciais de acesso ao Docker configuradas (INFRA-07):** criado o ficheiro `.env` local (não vai para o git) com utilizador e password de admin, e um ficheiro `.env.example` (vai para o git) como template para novos ambientes. O docker-compose foi atualizado para ler estas credenciais automaticamente.
- **Deploy testado numa máquina Debian 13 no Proxmox:** a app foi colocada a correr na VM `chichorro-staging` (192.168.0.7) com Docker. Os endpoints de saúde respondem corretamente e o cálculo de risco de incêndio funciona no browser.
- **Portainer instalado na VM:** ferramenta de gestão visual de Docker acessível em `http://192.168.0.7:9000`, permitindo monitorizar o estado dos contentores sem linha de comandos.

## Estado do projeto

A app CHICHORRO está agora containerizada e a funcionar num ambiente de staging real (VM Debian 13 no Proxmox). Isto significa que é possível testar novas versões num servidor independente antes de colocar em produção no Render. O deploy em produção continua a funcionar normalmente (Render + Cloudflare). O próximo passo natural é avançar para a próxima fase do roadmap VPS ou retomar funcionalidades pendentes como o FEAT-04 (exportação PDF) ou AUTH-09d (avatar WebP).

## Próximo passo

Decidir entre continuar o roadmap VPS (DB-07 — base de dados no servidor) ou avançar com uma funcionalidade de utilizador pendente.
