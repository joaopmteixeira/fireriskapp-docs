# Resumo do dia - 15 de junho de 2026

## O que foi feito hoje

- Completada a stack de staging na VM Proxmox (chichorro-staging, 192.168.0.7): Nginx como reverse proxy na porta 80 e PostgreSQL 17 local no Docker, eliminando a dependência do Supabase externo no ambiente de testes (INFRA-07)
- Adicionado sistema de backups automáticos ao PostgreSQL local: cópia diária às 02:00 com retenção de 7 dias e script de restore testado (DB-07)
- Criado script e runbook completo para migrar dados de produção (Supabase) para o PostgreSQL local do staging — validado com sucesso: 65 registos e 2 utilizadores migrados (DB-08)
- Exposta a aplicação na internet através do domínio `chichorro.joaopmteixeira.net` via Cloudflare Tunnel — sem abrir portas no router, com HTTPS automático e IP da VM nunca exposto (INFRA-09)
- Instaladas duas ferramentas de administração de base de dados (pgAdmin e Adminer) em paralelo no staging para avaliação — ambas funcionais e com guias de uso criados (INFRA-10)

## Estado do projeto

O ambiente de staging está completamente operacional e acessível publicamente em `https://chichorro.joaopmteixeira.net`. A stack inclui Nginx + FastAPI + PostgreSQL 17 com backups automáticos diários. Foi também preparada a rota de migração dos dados de produção para o servidor local. Estão instaladas duas ferramentas web de administração de base de dados (pgAdmin na porta 5050 e Adminer na porta 5051) para que o utilizador possa experimentar ambas e decidir qual manter. A produção continua no Render + Supabase sem alterações. O próximo passo é avançar para a segurança (SEC-11/SEC-12).

## Próximo passo

Decidir entre pgAdmin e Adminer (remover a não escolhida) e avançar para SEC-11 — gestão de secrets no servidor.
