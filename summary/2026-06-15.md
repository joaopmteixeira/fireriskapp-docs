# Resumo do dia - 15 de junho de 2026

## O que foi feito hoje

- Completada a stack de staging na VM Proxmox (chichorro-staging, 192.168.0.7): Nginx como reverse proxy na porta 80 e PostgreSQL local no Docker, eliminando a dependência do Supabase externo no ambiente de testes
- Configurado acesso SSH da VM ao GitHub (chave ed25519 gerada na VM e adicionada ao GitHub) para que os updates futuros sejam feitos com um simples `git pull` sem pedir credenciais
- Clonado o repositório na VM e arrancada a stack completa com `docker compose -f docker-compose.staging.yml up --build -d`
- Resolvido problema de password com caracteres especiais (`@`) no DATABASE_URL — solução: gerar passwords apenas com caracteres hexadecimais via `python3 -c "import secrets; print(secrets.token_hex(32))"`
- Adicionado lembrete automático no final de cada resposta para não esquecer de invocar o update dos TODOs quando um ID é concluído
- Bloqueado o uso da ferramenta Bash (via hook) para forçar sempre o uso de PowerShell neste projeto Windows

## Estado do projeto

O ambiente de staging está agora operacional com a stack completa: Nginx + FastAPI + PostgreSQL local. O workflow de update é `git pull` seguido de `docker compose -f docker-compose.staging.yml up --build -d`. A produção continua no Render + Supabase sem alterações. O próximo passo natural é DB-07 (backups do PostgreSQL local na VM).

## Próximo passo

Avançar para DB-07 — estratégia de backups para o PostgreSQL local no staging (pg_dump + upload para armazenamento externo + alertas de falha).
