# Resumo do dia — 1 de junho de 2026

## O que foi feito hoje

- **Grafo do código executado:** O Graphify foi instalado e corrido sobre toda a
  aplicação CHICHORRO — backend Python e frontend TypeScript em conjunto. Resultado:
  740 nós, 1657 ligações e 44 comunidades de ficheiros relacionados. Os "nós deus"
  (mais conectados) são `_get_db()`, `getAppStorage()` e `Request`.

- **Regras de uso documentadas:** Foi criado um ficheiro CLAUDE.md na raiz do
  repositório (local, não vai para o git) com a arquitetura resumida e as regras
  de quando refrescar o grafo — correr apenas em mudanças estruturais (ficheiros
  novos, routers/schemas/componentes), nunca em edições triviais.

- **Estratégia de IA formalizada no projeto:** As tarefas de IA foram separadas com
  prefixo próprio (AI-01 ✅, AI-02 ❌, AI-03 🔮) e registadas no Linear com os
  issues FIR-34 (AI-01) e FIR-35 (AI-02).

- **Plano do vault Obsidian entregue:** O documento `HANDOFF_OBSIDIAN_VAULT.md`
  especifica a estrutura completa a construir — 5 fatores, 27 subfatores, 9 fontes,
  scripts necessários e esquema de frontmatter para cada nota.

## Estado do projeto

O primeiro passo da estratégia de IA está completo: o grafo do código (Graphify)
funciona e dá uma vista estrutural navegável de toda a aplicação. A próxima camada
é o vault Obsidian (AI-02 / FIR-35) que vai ligar os 27 subfatores às fontes nas
dissertações e no regulamento RT-SCIE.

## Próximo passo

Implementar AI-02 — converter as dissertações para texto e gerar as 48 notas do
vault Obsidian com referências automáticas (FIR-35).
