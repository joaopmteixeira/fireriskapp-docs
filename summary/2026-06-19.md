# Resumo do dia - 19 de junho de 2026

## O que foi feito hoje

- **Mapa completo do projeto criado** — foi criado um novo ficheiro (`PROJECT_STATUS.md`) que lista todas as 97 tarefas do projeto numa única tabela, com o estado de cada uma, o link para o plano e a data de conclusão. Passa a ser a referência central para perceber o que está feito, em curso e por fazer.

- **Planos pendentes sinalizados visualmente** — os ficheiros de planeamento de tarefas ainda não implementadas foram renomeados com o sufixo `_UNDONE` (ex: `UI-12_UNDONE.md`). Assim é imediatamente visível quais os planos que existem mas cuja implementação ainda não começou.

- **Inconsistências nos TODOs corrigidas** — a lista de tarefas (`TODO_LIST.md` e `TODO_PRIORITIES.md`) tinha várias entradas com estado errado (tarefas concluídas marcadas como pendentes, entradas fora de ordem, IDs em falta). Tudo foi corrigido e verificado.

- **DB-06 confirmado e link corrigido** — verificou-se que o subplan do DB-06 (migração para SQLAlchemy ORM) já existia e estava completo. O único problema era um link errado no PROJECT_STATUS.md, que foi corrigido.

- **Ambiente de desenvolvimento local configurado** — o Python 3.12 foi adicionado permanentemente ao PATH do Windows, foi criado um utilizador de teste (`admin`/`admin`) para não ser necessário registar conta manualmente, e foi criado um script (`dev.ps1`) que arranca o projeto completo com um único comando e abre o browser automaticamente.

## Estado do projeto

O projeto está organizado e com a documentação consistente. Não foi feita nenhuma alteração ao código da aplicação hoje — o foco foi em arrumar a casa: corrigir o estado das tarefas, criar uma vista centralizada de tudo o que existe, e preparar o ambiente de desenvolvimento local para que seja possível testar alterações sem depender da VM ou do Render.

## Próximo passo

Implementar **UI-12** — Modal "Sobre" na LoginPage (apenas frontend, sem base de dados, subplan completo em `UI-12_UNDONE.md`).
